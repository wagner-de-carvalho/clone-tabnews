services:
  database:
    container_name: tabnews_database
    image: "postgres:16.0-alpine3.18"
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
    ports:
      - "5432:5432"
